{"kind": "fixed", "text": "a directory declared as dir/ covered nothing beneath it: resolve() stripped the slash and stored a single-file claim no write can land on, so the map granted agreements that meant less than the sentence typed and then nagged the agent for honoring them", "reproduced_first": true, "changes": ["resolve() reads a trailing-slash path, or any path that is a directory on disk, as its subtree; SPEC 1, why_bad and the declare_work doc now say so"], "evidence": [{"where": "stand-in", "what": "the incident reconstructed from the wrote-state of four repos (every nagged path sat under a dir/ declare, every exactly-spelt file was absent), then reproduced as a failing test before the fix turned it green; 48 tests pass", "gap": "the running MCP server still holds the old semantics until its next spawn - the relaunch that follows this commit is that spawn"}]}
